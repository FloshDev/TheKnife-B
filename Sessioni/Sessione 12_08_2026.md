# Sessione 12/08/2026 — Il server prende corpo, nove decisioni chiuse, il primo end-to-end come obiettivo

> **Versione definitiva.** Chiusa il 12/08 dopo il riscontro incrociato di Barlera e
> Scolaro sui DTO e su `Queries.sql`, verificato riga per riga sul codice reale (§1, §4,
> §6), e dopo la call di team che ha chiuso l'ultima scelta aperta (decisione 29). I numeri
> di §1 sono ricontati due volte prima di questa versione. S17 e S30 sono chiuse — S30
> tramite le decisioni 28 e 29 — S29 (segnalazione) resta aperta, assegnata a Scolaro.

## Perché questo documento

Il 10/08 il piano assegnava a Ciani quattro passi in sequenza. Due giorni dopo i passi 0, 1
e 2 sono chiusi e il passo 3 è a metà: `UtenteService` esiste e compila, i tre Command di
autenticazione no. Nel frattempo Barlera ha chiuso il client — dodici controller agganciati
— e ha alzato sei punti che richiedevano una decisione.

Il documento fa cinque cose:

1. **verifica** cosa è arrivato dal 10/08 a oggi, sul codice e sui commit;
2. **nomina il fatto centrale**: client e server non si sono mai parlati, e il motivo non è
   un bug ma due pezzi di cablaggio che nessuno ha scritto (§2);
3. **chiude le sei questioni sollevate da Barlera** con le decisioni 21–26, più la 27 e la
   28 che chiudono i due punti rimasti in sospeso (§4);
4. **rimette in ordine il piano**, con una checklist per persona (§7);
5. **registra il riscontro incrociato** di Barlera e Scolaro sui DTO e su `Queries.sql`,
   verificato riga per riga sul codice reale (§4, §6).

Dove un documento e il codice divergono, è segnalato come tale e non interpretato.

---

## 1. Stato verificato al 12/08

33 commit dalla fine del documento di sessione del 10/08 (`e225449`, "Aggiunta checklist
sessione", 10/08 16:58) a oggi. Ripartizione per autore:

| Membro | Commit | Cosa è arrivato |
|--------|--------|-----------------|
| **Barlera** | 11 | Dodici controller agganciati, `TaskRunner`, risposta a recensione, JavaDoc sui campi `@FXML`, diagrammi aggiornati |
| **Scolaro** | 11 | `RecensioneDAO` (7 metodi), **`PreferitoDAO`** (3 metodi), `trovaCoordinateLuogo`, `DataAccessException` applicata, `Schema.sql` e `Role.sql` aggiornati |
| **Ciani** | 6 | `DataAccessException`, collaudo del layer DAO, fail-fast sulla connessione al DB, `UtenteService`, documenti di sessione |
| **Gasparini** | 5 | Correzioni al contratto del protocollo, manuale tecnico iniziato |

Nota sul conteggio: verificato due volte in questa sessione, con esito diverso ogni volta —
prima bozza sommava a 24 invece di 35, un secondo tentativo (fatto da Ciani in parallelo)
sommava a 35 ma con Ciani stesso a 8 invece di 6. Il numero qui sopra (33) è il terzo
conteggio, rifatto da zero con `git log "e225449..HEAD" --format='%an' | sort | uniq -c`
il 12/08. Usando invece `--since="2026-08-10"` (taglio per data di calendario anziché per
commit) si ottengono 39 commit, con Barlera a 14: la differenza sono i commit del 10/08
stesso, precedenti alla chiusura di quel documento. Le identità git contate sono
`Marco Barlera`, `Dev-Scola`, `FloshDev`, `Gaspa999`; l'identità alternativa di Scolaro
(`gabriscolaro151-commits`) non compare in questa finestra (S18).

### Client — Barlera

Dodici controller in `client.ui`, tutti agganciati: `Splash`, `Login`, `Registrazione`,
`Home`, `Risultati`, `Dettaglio`, `ScriviRecensione`, `GestioneRecensioni`, `Preferiti`,
`Dashboard`, `AggiungiRistorante`, `AssociaRistorante`. Più `TaskRunner`, che tiene la GUI
reattiva mandando le chiamate di rete fuori dal thread JavaFX.

Il client è, in superficie, **completo**: tutti e venti i `CommandType` hanno un chiamante.

### Server — Scolaro

`RecensioneDAO` consegnato con 7 metodi (`aggiorna`, `elimina` e `rispondi` compresi, tutti
e tre filtrati solo per id — l'ownership sta nel service, non nel DAO). `RistoranteDAO`
arriva a 8, incluso `trovaCoordinateLuogo` (decisione 17). `PreferitoDAO`, il sesto file del
package, è arrivato il 12/08 con `aggiungi`, `rimuovi`, `trovaPerCliente`: **S17 chiusa**.
`DataAccessException` applicata su tutte le firme; l'unica eccezione voluta è
`RecensioneDAO.inserisci()`, che dichiara **anche** `ApplicationException` per il vincolo
di unicità cliente/ristorante — corretto, ed è la risposta alla segnalazione che Barlera
aveva sollevato (punto 6 della sua lista, **chiuso**).

`@author` presente su tutti e sei i file del package `dao`. Il layer dati è completo; resta
solo da riallineare `Queries.sql` al codice reale (S29).

### Protocollo — Gasparini

`ContrattoProtocolloCS.md:30` — la riga `CERCA_VICINO` ora dichiara *Token: no*: **S20
chiusa**. La tabella di autorizzazione riga 55 classifica `ESCI` sotto *cliente/ristoratore*
e `CommandType.java` lo raggruppa sotto `// CLIENTE E RISTORATORE`: **S21 chiusa nel
codice**, con un residuo documentale (§6, S28).

Manuale tecnico iniziato.

### Server — Ciani

Passi 0, 1 e 2 della checklist 10/08 chiusi (dettaglio in §3). Passo 3 a metà.

---

## 2. Il fatto centrale: nessun byte è mai passato fra client e server

Il 10/08 il fatto centrale era che il layer DAO non fosse mai stato eseguito. Il collaudo
l'ha risolto. Il fatto centrale di oggi è un gradino più in alto, e riguarda **entrambi i
lati**.

### Lato client — il socket non viene mai aperto

`ServerConnection.java:50` espone:

```java
public void connect(String host, int port) throws IOException
```

La firma è già parametrica. Ma `grep -rn "\.connect(" theknife-client` restituisce **zero
risultati**. Non è il caso di un `localhost` cablato da correggere: manca proprio il
chiamante. Il `Socket` è `null` per tutta la vita dell'applicazione.

Ne segue che ogni `inviaRichiesta()` dei dodici controller, se eseguito, fallisce prima di
toccare la rete.

### Lato server — i service non ricevono i DAO

`Server.java:42`:

```java
RistoranteService ristoranteService = new RistoranteService();
```

Costruttore senza argomenti, perché `RistoranteService` è **uno stub**: nessun campo,
`cercaRistoranti()` ritorna `new ArrayList<>()` incondizionatamente. Quindi
`CercaRistorantiCommand` — l'unico Command esistente, l'unico cablato nella
`CommandFactory` — risponde **sempre** `NON_TROVATO`, anche con 17 737 righe nel database.

`UtenteService`, scritto ieri e funzionante, **non viene istanziato da nessuna parte**.
Nessun `UtenteDAO`, `RistoranteDAO`, `ServizioDAO` o `RecensioneDAO` viene costruito fuori
dal collaudo: l'unico `new DatabaseManager` in `main/` è quello del retry di connessione,
che apre una `Connection` di prova e la chiude.

### Perché conta

I due difetti sono simmetrici e nessuno dei due si vede compilando. Il progetto ha oggi
**un client completo che non sa dove chiamare** e **un server che ascolta ma non ha niente
dietro**. Le due metà non si sono mai toccate.

Il primo end-to-end reale non è un traguardo lontano: sono due sessioni di cablaggio. Ma
finché non avviene, ogni riga scritta da qui in poi è codice mai eseguito — la stessa
condizione che il 10/08 abbiamo pagato sul layer DAO.

**Priorità di questa settimana: il primo `ACCEDI` che parte da una GUI e torna con un
token.** Tutto il resto viene dopo.

---

## 3. Esito della checklist 10/08 — Ciani

### Passo 0 — collaudo del layer dati ✅

- ✅ Import su Postgres locale: `Role.sql` → `Schema.sql` → `Data.sql`, senza errori
- ✅ `main` di collaudo che istanzia `DatabaseManager` con le credenziali `tk_app` e chiama
  i quattro metodi previsti
- ✅ Verificate le due trappole: i ristoranti senza recensioni compaiono (`LEFT JOIN`), gli
  id distinti coincidono col totale (nessun duplicato da servizi N:N)
- ✅ Difetti trovati segnalati a Scolaro, corretti da lui dentro `server/dao/`

**S13 chiusa.** Con un residuo: il collaudo doveva essere usa-e-getta e non committato, ed
è invece finito in `src/test/java` (`bc37318`). Vedi S25.

### Passo 1 — sbloccare gli altri ✅

- ✅ `DataAccessException` + push immediato (`e466a8f`). Scolaro l'ha applicata su tutte le
  firme entro la giornata

### Passo 2 — avvio del server ✅

- ✅ S5: `sc.close()` rimosso da `ConfigurazioneDB.leggiDaTerminale()`
- ✅ `Server.avvia()`: connessione di prova prima del `ServerSocket`
- ✅ Retry max 3 con messaggi distinti per `28P01` / `28000` / `3D000` / `08001`

**S5 chiusa.**

### Passo 3 — autenticazione end-to-end — 2 voci su 6

- ✅ `at.favre.lib:bcrypt` nel `pom.xml` di `theknife-server` — **S4 chiusa**
- ✅ `UtenteService`: `accedi()` su `trovaHashPassword` + verifica BCrypt, `registra()` con
  hash a costo 12, `esci()` sul `SessionManager`
- ❌ `AccediCommand`
- ❌ `RegistratiCommand`, `EsciCommand`
- ❌ Registrazione dei tre in `CommandFactory`
- ❌ Verifica di sicurezza: `SELECT password FROM Utenti` deve mostrare `$2a$`/`$2b$`
- ❌ Primo end-to-end reale

### Passo 4 — service e comandi rimanenti — 0 voci su 7

Nessuna voce iniziata. `RistoranteService` è ancora lo stub del 04/08.

---

## 4. Decisioni chiuse in questa sessione

**Chiuse il 12/08. Non si riaprono**, come D1–D5, 1–6, 7–14 e 15–20.

Dalla 22 alla 26 nascono dalla lista di sei punti alzata da Barlera; la 21 da una domanda
emersa progettando `RistoranteService`; la 27 chiude S26, il punto che la 22 aveva lasciato
aperto.

| # | Tema | Decisione |
|---|------|-----------|
| **21** | Dove vivono i preferiti lato server | **Dentro `RistoranteService`**, non in un `PreferitoService` separato. Il service prende un terzo DAO (`PreferitoDAO`) e tre metodi. Simmetrico al client, dove i tre comandi stanno già in `client/service/RistoranteService.java` (decisione I3, tre service e basta) |
| **22** | Connessione del client al server (punto 1 di Barlera) | **Automatica all'avvio, senza interazione dell'utente.** Nessun dialog di configurazione: `clientTK` apre il socket da solo prima di mostrare lo splash. Origine di host e porta: vedi S26 |
| **23** | Utente corrente lato client (punto 2, ex S06) | **In `ServerConnection`, accanto al `sessionToken`.** È già il Singleton che tiene lo stato di sessione; l'`UtenteDTO` ha lo stesso ciclo di vita del token e viene svuotato con lui al logout. Nessuna classe `SessionContext` aggiuntiva |
| **24** | Autorizzazione sulla risposta a recensione (punto 3) | **Solo il gestore del ristorante può rispondere a una recensione di quel ristorante.** Controllo **lato server**, dentro `RispondiRecensioneCommand`: è ownership, quindi non lo copre il gate del Dispatcher (decisione 20). Il filtro nella UI è cosmetico e non sostituisce il controllo |
| **25** | Serializzazione delle richieste sul socket (punto 4) | **Il `TaskRunner` resta a thread singolo, ed è una garanzia di protocollo, non una preferenza.** Un socket, un `ObjectOutputStream`, un thread che ci scrive: le Request si accodano, mai due in volo. Chi in futuro allarga quell'executor rompe il protocollo. Va documentato nel codice, non solo qui |
| **26** | Nome leggibile della località (punto 5) | **`PosizioneDTO` porta anche il nome della località**, non solo latitudine e longitudine. Il server lo ricava per reverse geocoding e lo restituisce già pronto. Il client non fa geocoding |
| **27** | Origine di host e porta lato client (chiude S26) | **Argomenti opzionali della riga di comando, con default in `theknife-common`.** `java -jar clientTK.jar [host] [porta]`; senza argomenti, `localhost` e la porta di default. Il valore di default vive in **un solo punto**, una classe di `theknife-common` letta sia dal client sia dal server: cambiarlo lì si propaga a cascata a entrambi. Chiude anche S27 |
| **28** | Indirizzo del ristorante in inserimento (chiude S30, parte 1) | **L'indirizzo è obbligatorio all'aggiunta di un ristorante, e quindi lo è anche la provincia.** `AggiungiRistoranteDTO` acquista il campo `provincia`, che oggi non ha; `RistoranteDAO.inserisci()` smette di scrivere `NULL` su quella colonna. Il form di inserimento lato client valida la presenza dei campi dell'indirizzo prima di inviare la Request |
| **29** | Coordinate del ristorante in inserimento (chiude S30, parte 2) | **Geocoding lato server, nessun campo lat/lon nel DTO.** Deciso in call il 12/08. `RistoranteService.aggiungi()` geocodifica l'indirizzo prima di passare i dati a `RistoranteDAO.inserisci()`, riusando `server.external.GeocodingClient` (decisione 17/26) con fallback su `trovaCoordinateLuogo` se il servizio esterno non risponde. Il gestore compila solo l'indirizzo, non conosce le proprie coordinate e non deve inserirle |

### Conseguenze della 21 — il costruttore di `RistoranteService`

Il service passa a **tre DAO** (`RistoranteDAO`, `ServizioDAO`, `PreferitoDAO`) e **dieci
metodi**. Il costruttore va deciso prima di scrivere il corpo, perché lo stesso costruttore
compare in `Server.avvia()` e nella `CommandFactory`.

I dieci metodi, ricavati dai `CommandType` incrociati coi metodi DAO esistenti:

| CommandType | Metodo | DAO sotto |
|---|---|---|
| `CERCA_RISTORANTI` | `cercaRistoranti(CercaRistorantiDTO)` | `RistoranteDAO.cerca()` |
| `OTTIENI_DETTAGLI_RISTORANTE` | `dettagli(long)` | `trovaPerId()` |
| `CERCA_VICINO` | `cercaVicino(CercaVicinoDTO)` | `cercaVicino()` |
| `AGGIUNGI_RISTORANTE` | `aggiungi(AggiungiRistoranteDTO, long)` | `inserisci()` + `ServizioDAO.associa()` |
| `VEDI_RISTORANTI_GESTITI` | `ristorantiGestiti(long)` | `trovaPerGestore()` |
| `OTTIENI_STATISTICHE_RISTORANTE` | `statistiche(long)` | `statistiche()` |
| `ASSOCIA_RISTORANTE` | `associaGestore(long, long)` | `associaGestore()` |
| `AGGIUNGI_PREFERITO` | `aggiungiPreferito(long, long)` | `PreferitoDAO` |
| `RIMUOVI_PREFERITO` | `rimuoviPreferito(long, long)` | `PreferitoDAO` |
| `VEDI_PREFERITI` | `preferiti(long)` | `PreferitoDAO` |

I tre metodi sui preferiti **dipendono da S17** e vanno scritti per ultimi: gli altri sette
non aspettano nessuno.

Nota sulle firme: per la decisione 16 il `throws DataAccessException` **attraversa** il
service senza essere catturato, come già in `UtenteService.accedi()`. E `UnauthorizedException`
**esce** dalle firme: per la decisione 20 l'autorizzazione sta nel Dispatcher e l'ownership
nel Command, non nel service. La firma attuale di `cercaRistoranti` va corretta di
conseguenza.

### Conseguenze della 22 — cosa vuol dire «automatica»

`clientTK` non chiede niente all'utente: apre il socket all'avvio e, se fallisce, mostra un
errore leggibile e termina. Nessun dialog host/porta.

Da dove arrivano host e porta era il punto aperto S26: **lo chiude la decisione 27**, qui
sotto. Il vincolo che lo rendeva non banale è DIRETTIVE riga 43:

> «supportare l'interazione in parallelo con più utenti connessi alla piattaforma da
> postazioni (client) differenti»

Un `localhost` cablato nel sorgente rende impossibile il collaudo del docente su due
macchine.

### Conseguenze della 27 — un solo punto per la porta

Le tre forme compatibili con «automatica» erano: argomenti della riga di comando con
default; un file `.properties` accanto al jar; una costante con override da variabile
d'ambiente. Scelti gli **argomenti**, per tre ragioni concrete:

- JavaFX li consegna già. `launch(args)` in `TheKnifeClient:26` li rende leggibili con
  `getParameters().getRaw()` dentro `start()`. Nessuna dipendenza, nessun file nuovo.
- Il `.properties` andrebbe cercato sul **filesystem** accanto al jar, non con
  `getResource()`, che guarda *dentro* il jar: la risoluzione del path dipende dalla
  directory da cui si lancia il comando. E per cambiare host il docente deve aprire ed
  editare un file.
- La variabile d'ambiente ha sintassi diversa per sistema operativo (`export` contro
  `setx`): contro il requisito multipiattaforma, e si documenta male nel `README.txt`.

Senza argomenti resta `localhost` e la porta di default, quindi il doppio clic sul jar
continua a funzionare per la demo su una macchina sola. Un argomento opzionale non è un
dialog: la decisione 22 regge.

**Il default vive in un punto solo.** Oggi `PORTA_SERVER = 9999` è una costante privata di
`Server.java:25` e il client dovrebbe ricablare lo stesso numero per conto suo: due costanti
indipendenti che devono restare uguali per sempre, prima o poi divergono. Va in una classe
di `theknife-common` — entrambi i moduli ci dipendono già — e da lì la leggono sia
`Server.avvia()` sia `TheKnifeClient.start()`. Cambiarla lì cambia tutto a cascata.
Simmetria col server: anche la porta di ascolto del server prende lo stesso default e
accetta un argomento opzionale.

Convenzione del nome: precedente in casa è `ConfigurazioneDB`, quindi italiano.

**Tre cose da non saltare quando si scrive.** La connessione automatica può fallire, e senza
gestione fallisce in silenzio o con uno stack trace in console che nella GUI nessuno vede:

- `Integer.parseInt` su un argomento non numerico lancia `NumberFormatException`: va
  intercettata, messaggio leggibile e non traccia.
- `connect()` fallito → errore visibile che dice **quale** host:porta è stato tentato e la
  sintassi per riavviare. **Non** un dialog che chiede l'host: quello riaprirebbe di fatto
  la decisione 22.
- `disconnect()` (`ServerConnection:57`) va in `NullPointerException` se il socket non è mai
  stato aperto — cioè proprio nel caso in cui `connect()` è fallito. Serve una guardia. E
  oggi non lo chiama nessuno: va agganciato all'override di `Application.stop()`.

La connessione si apre in `TheKnifeClient.start()`, non nello `SplashController`: è ciclo di
vita dell'applicazione, non di una schermata. La firma di `connect(String, int)` è già
giusta, non cambia una riga.

### Conseguenze della 28 — il DTO cresce di un campo, e ne resta uno scoperto

Lo stato verificato: `AggiungiRistoranteDTO` ha 11 campi (`nome`, `indirizzo`, `citta`,
`nazione`, `fasciaPrezzo`, `prenotazioneOnline`, `consegnaADomicilio`, `tipoCucina`,
`website`, `telefono`, `premi`, più la lista dei servizi). La tabella `RistorantiTheKnife` ne
ha tre in più che il DTO non porta: `provincia`, `latitudine`, `longitudine`. Per questo
`RistoranteDAO.inserisci()` ci scrive `NULL`, con un commento esplicito nel codice.

La decisione 28 chiude la parte sull'indirizzo: **la provincia entra nel DTO**, perché fa
parte dell'indirizzo e l'indirizzo è obbligatorio. È un cambio di DTO serializzato — stessa
cautela della decisione 26: chi non ripulla e ricompila ottiene `InvalidClassException` a
runtime, non in compilazione. Le due modifiche conviene farle e annunciarle insieme.

### Conseguenze della 29 — chi geocodifica e quando

La metà scoperta dalla decisione 28 era la ragione per cui era nata S30: la provincia non
risolve da sola il problema. `RistoranteDAO.cercaVicino()` filtra
`WHERE t.latitudine IS NOT NULL AND t.longitudine IS NOT NULL`, quindi senza coordinate un
ristorante inserito da un gestore non sarebbe mai comparso fra i risultati di `CERCA_VICINO`
— un requisito che avrebbe funzionato solo sulle righe importate dal CSV del docente, mai su
quelle inserite dall'applicazione. Chiusa in call il 12/08: **geocoding lato server**,
nessun campo lat/lon nel DTO.

Punto di innesto: `RistoranteService.aggiungi(AggiungiRistoranteDTO, long idGestore)`,
**prima** di chiamare `RistoranteDAO.inserisci()`. Ordine dei tentativi, riusando
l'infrastruttura già scritta per la decisione 17/26:

1. `GeocodingClient` (Nominatim) sull'indirizzo completo (via + città + nazione)
2. se non risponde o non trova nulla, fallback su `RistoranteDAO.trovaCoordinateLuogo(citta)`
   — media delle coordinate dei ristoranti già presenti in quella città
3. se anche questo non trova nulla (prima città inserita, nessun riferimento), `latitudine`
   e `longitudine` restano `NULL` e il ristorante non comparirà in `CERCA_VICINO` finché
   qualcuno non lo modifica: **non è un errore che blocca l'inserimento**, il ristorante
   viene comunque creato.

Va scritto **prima** di `AggiungiRistoranteCommand`, perché cambia la firma di
`RistoranteService.aggiungi()`: chi la scrive senza saperlo la firma come oggi (solo DTO +
idGestore) e poi deve rifarla.

### Conseguenze della 23 — cosa cambia davvero

`AuthService.java:34` oggi salva solo il token e **scarta** `loginResultDTO.getUtente()`.
La correzione è: un campo `utenteCorrente` in `ServerConnection`, un getter, una riga in
`AuthService`, una nell'azzeramento del logout. Mezz'ora.

Ma sblocca tre requisiti fermi: RF08, RF09 (icona preferiti dinamica) e RF12 (Modifica /
Elimina solo sulle recensioni proprie). Barlera aveva chiesto se aspettare
l'architettura — **non c'è un'architettura da aspettare**, è un campo. Si fa subito e i due
schermi si scrivono già col filtro di proprietà, senza doppio passaggio.

### Conseguenze della 24 — la UI non è una barriera

RF18 «Rispondi» è già implementato e funzionante lato client, **senza** filtro per
proprietà: dalla GUI chiunque può rispondere a qualunque recensione.

Va detto in chiaro, perché vale per tutti e tre i requisiti sopra: **nascondere un pulsante
non è autorizzazione.** Un client modificato manda la Request comunque. Il filtro nella UI
serve a non confondere l'utente; a rifiutare servono il gate del Dispatcher (decisione 20) e
i controlli di ownership dentro i Command (questa decisione).

`RispondiRecensioneCommand` deve quindi: risalire dalla recensione al ristorante, dal
ristorante al gestore, e confrontarlo con l'`idUtente` della sessione. Se non coincide,
`NON_AUTORIZZATO`. Stessa forma per `MODIFICA_RECENSIONE` ed `ELIMINA_RECENSIONE`, dove il
proprietario è l'autore della recensione.

**Su modifica ed eliminazione c'è un dettaglio da non perdere.** Il progetto originale
metteva l'ownership nella query: `Queries.sql:274` e `:281` filtrano ancora
`WHERE id_recensione = ? AND id_cliente = ?`. I DAO reali no — `RecensioneDAO:180` e `:202`
filtrano solo su `id_recensione`. È la scelta corretta, coerente con la decisione 20
(l'autorizzazione non sta nel layer dati), ma sposta il controllo altrove **senza che
nessuno l'abbia ancora scritto**: oggi in `server/service/` ci sono solo `RistoranteService`,
`UtenteService` e `SessionManager`, e in `server/command/` solo `Command` e
`CercaRistorantiCommand`.

Non è una falla aperta oggi — `Modifica`/`EliminaRecensioneCommand` non esistono, quindi non
c'è niente da sfruttare. È un debito da saldare **nello stesso momento in cui si scrivono**:
chi li scrive senza sapere questo produce un comando che lascia a chiunque modificare la
recensione di chiunque, e il DAO non lo ferma. La parte documentale (`Queries.sql` da
riallineare) è S29.

### Conseguenze della 25 — dove va scritta

Due righe di JavaDoc sopra il campo `executor` di `TaskRunner`, che dicano **perché** è a
thread singolo: un solo socket condiviso, un solo `ObjectOutputStream`, due Request in
parallelo corromperebbero lo stream. Il sintomo di una violazione sarebbe una
`ClassCastException` casuale in lettura, non un errore leggibile — motivo per cui il
commento vale più del verbale.

Segnalazione corretta da parte di Barlera: era la più fragile delle sei, perché è una
garanzia che non si vede leggendo il codice che la usa.

### Conseguenze della 26 — cosa cambia nel DTO e chi lo paga

`PosizioneDTO` oggi ha due campi, `latitudine` e `longitudine`. Ne acquista un terzo per il
nome della località.

Il valore lo produce il server: `server.external.GeocodingClient` (decisione 17) parla già
con Nominatim/OpenStreetMap per il geocoding diretto; il reverse è lo stesso servizio su un
endpoint diverso. **Nessuna dipendenza nuova** — cosa che conta, perché ogni libreria in più
va poi impacchettata in `lib/` e giustificata nel `README.txt` di consegna (decisione 19).
Se invece si vuole la libreria usata l'anno scorso, va prima identificata e verificata la
licenza; l'assistente non ha elementi per dire quale fosse.

Le stesse regole del geocoding diretto valgono qui: `User-Agent` esplicito, non più di una
richiesta al secondo, timeout ~2 s, e fallimento **silenzioso** — se il reverse non
risponde, il campo resta `null` e la GUI mostra il campo di ricerca vuoto, non le coordinate
grezze.

⚠️ **Cambiare `PosizioneDTO` è un cambio di contratto serializzato.** Chi non ripulla e
ricompila ottiene una `InvalidClassException` **a runtime**, non in compilazione. Va
annunciato al gruppo nel momento in cui viene pushato.

---

## 5. Cosa il docente chiede davvero sulla località

Emerso verificando il punto 5 di Barlera, e vale la pena metterlo a verbale perché
ridimensiona una funzionalità che stavamo trattando come centrale.

DIRETTIVE riga 50:

> «proseguire come utente *guest* indicando solo il nome di un luogo»

Il nome del luogo **lo digita l'utente**. Da nessuna parte è richiesto che il sistema
indovini dove si trova. La geolocalizzazione da IP (`OTTIENI_LOCALITA_INIZIALE`, decisione
18) è una comodità che ci siamo aggiunti noi, e il gruppo conferma di volerla mantenere —
come *suggerimento* precompilato, per l'ospite e in generale nelle fasi di ricerca.

Resta un suggerimento: se fallisce, l'applicazione deve funzionare identica. Il percorso
obbligatorio è quello digitato a mano, ed è quello che il docente collauderà.

---

## 6. Segnalazioni — quadro al 12/08

| # | Segnalazione | Stato 10/08 | Stato 12/08 | Owner |
|---|--------------|-------------|-------------|-------|
| S4 | Hash password | ✅ chiusa, BCrypt non nel pom | ✅ **chiusa** — `bcrypt` in `theknife-server/pom.xml:32`, usato in `UtenteService` | — |
| S5 | `sc.close()` rompe il retry | 🔴 aperta | ✅ **CHIUSA** | — |
| S8 | Nessun controllo di autorizzazione | ✅ chiusa come decisione 20 | 🔴 **da implementare** — nessuna riga scritta. `CommandDispatcher.dispatch()` risolve la sessione e invoca il Command senza alcun gate | Ciani |
| S10 | Layout di consegna §4 non prodotto | 🟡 bassa | 🟡 invariata | Ciani (PM) |
| S13 | Layer DAO mai eseguito | 🔴 priorità massima | ✅ **CHIUSA** — collaudo eseguito, difetti segnalati e corretti | — |
| S17 | `RecensioneDAO` + `PreferitoDAO` + `trovaCoordinateLuogo` | 🟡 aperta | ✅ **CHIUSA** — `PreferitoDAO` pushato da Scolaro il 12/08 (commit `b53ce04`, HEAD di questa sessione): `aggiungi`, `rimuovi`, `trovaPerCliente`. La query di `trovaPerCliente` seleziona tutte e 18 le colonne di `RistoranteDTO`, media stelle e conteggio recensioni compresi — non le 8 di `Queries.sql` (vedi S29) | — |
| S18 | Identità git non unificata (email multiple nella cronologia) | 🟡 aperta | 🟡 **ridotta** — la parte sulle matricole è **infondata**: `autori.txt` è corretto, verificato da Ciani il 12/08. Resta solo l'unificazione delle email | Scolaro |
| S20 | Riga `CERCA_VICINO`: *Token: sì* | 🟡 nuova | ✅ **CHIUSA** — contratto riga 30 ora *no* | — |
| S21 | Riga `ESCI` sotto *cliente* | 🟡 nuova | ✅ **chiusa nel codice** — residuo documentale in S28 | — |
| **S22** | **`ServerConnection.connect()` non è invocato da nessuna riga del client: il socket non viene mai aperto** | — | 🔴 **NUOVA, bloccante** — copre la decisione 22 | Barlera |
| **S23** | **Cablaggio del server assente**: `Server.avvia()` istanzia `RistoranteService` senza DAO, `UtenteService` non è mai costruito, nessun DAO esiste fuori dal collaudo | — | 🔴 **NUOVA, bloccante** | Ciani |
| **S24** | `RistoranteService` è uno stub: `cercaRistoranti()` ritorna lista vuota, quindi `CercaRistorantiCommand` risponde sempre `NON_TROVATO` | — | 🔴 **NUOVA** — è il passo 1 di Ciani | Ciani |
| **S25** | `CollaudoDAO` committato in `src/test/java`, benché il piano lo prevedesse usa-e-getta e non committato | — | 🟡 **NUOVA** — decidere se tenerlo come collaudo manuale documentato o rimuoverlo prima della consegna | Ciani (PM) |
| **S26** | Origine di host e porta lato client non fissata: la decisione 22 stabilisce *automatica*, non *da dove* | — | ✅ **NUOVA e CHIUSA in giornata** — decisione 27: argomenti opzionali, default in `theknife-common` | — |
| **S27** | `PORTA_SERVER = 9999` costante privata in `Server.java:25`, mai verbalizzata e non nota al client | — | ✅ **NUOVA e CHIUSA in giornata** — la decisione 27 la sposta in `theknife-common`, letta da entrambi. Resta da annotare nel `README.txt` (decisione 19) | Ciani |
| **S28** | `ContrattoProtocolloCS.md:11`: lo snippet dell'enum tiene ancora `ESCI` e `CERCA_VICINO` sotto `// CLIENTE`, mentre `CommandType.java` e la tabella di autorizzazione (riga 54–55) sono corretti | — | 🟡 **NUOVA** — residuo di S21, correzione documentale | Gasparini |
| **S29** | **`Queries.sql` documenta un comportamento che i DAO non hanno più**, in tre punti verificati riga per riga: righe 274 e 281 tengono `AND id_cliente = ?` su modifica ed eliminazione recensione, mentre `RecensioneDAO:180` e `:202` filtrano solo `WHERE id_recensione = ?`; riga 136 tiene `LIMIT ?` su `cercaVicino`, mentre `RistoranteDAO:349` filtra `WHERE t.distanza_km <= ?`; riga 242 tiene una `visualizzaPreferiti` a 8 colonne, mentre `PreferitoDAO.trovaPerCliente` ne seleziona 18 | — | 🟡 **NUOVA** — il codice è giusto, il file è vecchio: si riallinea il file, non i DAO. Alzata dal riscontro incrociato Barlera/Scolaro | Scolaro |
| **S30** | `AggiungiRistoranteDTO` non porta `provincia`, `latitudine`, `longitudine`, che la tabella `RistorantiTheKnife` ha e che `RistoranteDTO` espone. `RistoranteDAO.inserisci()` scriveva `NULL` su quelle tre colonne | — | ✅ **NUOVA e CHIUSA in giornata** — decisione 28 (provincia nel DTO) + decisione 29 (coordinate via geocoding lato server, deciso in call) | — |

### Cosa è davvero aperto

| Aperta | Cos'è | Chi |
|--------|-------|-----|
| **S22, S23** | cablaggio mancante — **bloccano il primo end-to-end** | Barlera, Ciani |
| **S24** | `RistoranteService` da scrivere | Ciani |
| **S8** | implementazione della decisione 20 | Ciani |
| **S29** | `Queries.sql` da riallineare ai DAO in tre punti | Scolaro |
| **S25, S28, S18** | igiene e correzioni documentali | vari |
| S10 | layout di consegna, maturerà a fine progetto | Ciani (PM) |

**Nessuna scelta resta aperta.** S26 chiusa dalla decisione 27, S30 chiusa dalle decisioni
28 e 29 (l'ultima, in call il 12/08). S17 è chiusa: `PreferitoDAO` è arrivato mentre questo
documento veniva scritto. Da qui in avanti è tutto lavoro assegnato, nessuno aspetta una
risposta.

---

## 7. Checklist per persona

### CIANI — server: i service, il cablaggio, i comandi

L'ordine è vincolato: il passo 0 rende eseguibile tutto ciò che viene dopo, e senza il
passo 1 nessuno può provare niente.

#### Passo 0 — `RistoranteService` (in corso)

- [ ] Campi e costruttore: tre DAO iniettati (decisione 21), tutti `final`, nessuno stato
      mutabile — l'oggetto è condiviso fra i 20 thread del pool
- [ ] Correggere la firma di `cercaRistoranti`: via `UnauthorizedException` (decisione 20),
      dentro `DataAccessException` che attraversa (decisione 16)
- [ ] I 7 metodi che non dipendono da `PreferitoDAO` (tabella in §4)
- [ ] I 3 metodi sui preferiti — **non aspettano più niente**: `PreferitoDAO` è arrivato
      (`aggiungi`, `rimuovi`, `trovaPerCliente`), S17 chiusa
- [ ] ⛔ **`aggiungi()` geocodifica prima di inserire** (decisione 29): `GeocodingClient`
      sull'indirizzo, fallback `RistoranteDAO.trovaCoordinateLuogo(citta)`, altrimenti
      `NULL` senza bloccare l'inserimento. Va scritto insieme al metodo, non dopo — cambia
      la sua firma
- [ ] JavaDoc su ogni metodo pubblico (DIRETTIVE §3.2)

Chiude S24.

#### Passo 1 — cablaggio del server (subito dopo, stessa giornata)

- [ ] `Server.avvia()`: costruire i DAO dal `DatabaseManager` già validato dal retry, poi i
      service, poi la `CommandFactory`
- [ ] `UtenteService` istanziato e passato alla `CommandFactory`
- [ ] Verificare che il `DatabaseManager` del retry sia **lo stesso** che finisce nei DAO,
      non un secondo oggetto costruito sulla stessa configurazione

Chiude S23. **È il gemello di S22: senza entrambi non esiste end-to-end.**

#### Passo 2 — i tre Command di autenticazione

- [ ] `AccediCommand`: token già generato dentro `UtenteService.accedi()`, va solo passato
      nella `Response` (decisioni 12 e 13)
- [ ] `RegistratiCommand` — Response **senza payload** (decisione 15)
- [ ] `EsciCommand`
- [ ] Registrazione dei tre nella `CommandFactory`
- [ ] **Verifica di sicurezza**: dopo una registrazione, `SELECT password FROM Utenti` deve
      mostrare una stringa che inizia per `$2a$` o `$2b$`, non la password digitata
- [ ] **Primo end-to-end reale**: `serverTK` + `clientTK`, login dalla GUI

⚠️ Prima del test: **tutti devono aver ripullato**. I DTO sono cambiati l'08/08 e cambiano
ancora con la decisione 26; un DTO disallineato produce `InvalidClassException` a runtime,
non in compilazione.

#### Passo 3 — i service rimanenti

- [ ] `RecensioneService` sopra `RecensioneDAO` — **non è più bloccato**, il DAO è arrivato
- [ ] `server.external.GeocodingClient` (Nominatim): geocoding diretto + **reverse**
      (decisione 26), `User-Agent`, 1 req/s, timeout ~2 s, fallback su
      `trovaCoordinateLuogo` (decisione 17)
- [ ] Terzo campo in `PosizioneDTO` + annuncio al gruppo al momento del push (decisione 26)

#### Passo 4 — autorizzazione e comandi rimanenti

- [ ] **Gate nel `CommandDispatcher`** (decisione 20): struttura a tre stati — nessun
      requisito / autenticato qualunque ruolo / autenticato con ruolo specifico — ricavata
      dalla tabella di Gasparini. Se il gate non passa, `NON_AUTORIZZATO` **senza istanziare
      il Command**. Chiude S8
- [ ] Ownership dentro i Command: `RispondiRecensioneCommand` risale ristorante → gestore
      (decisione 24); `Modifica`/`EliminaRecensioneCommand` confrontano l'autore
- [ ] I Command rimanenti fino a coprire i 20 `CommandType`
- [ ] Test di concorrenza: 2+ client simultanei, verificare che le sessioni non si mescolino

#### Da PM — decisioni e igiene

- [x] ~~Chiudere S26: da dove arrivano host e porta~~ → **decisione 27**, chiusa in giornata
- [ ] ⛔ Portare il default di host e porta in `theknife-common` e farlo leggere a
      `Server.avvia()` al posto di `PORTA_SERVER` (`Server.java:25`). **Sblocca Barlera**:
      finché la costante non esiste, il client non ha da dove leggerla. Chiude S27
- [ ] Argomento opzionale della porta di ascolto anche lato server, per simmetria
      (`TheKnifeServer.main()`)
- [ ] Decidere su S25: `CollaudoDAO` resta come collaudo manuale documentato o si rimuove
- [ ] Annotare nel `README.txt` di consegna la sintassi `java -jar clientTK.jar [host]
      [porta]` e il default (decisione 19)
- [ ] `.mailmap` in radice (S18)
- [ ] Cancellare i residui in `Progettazione/Ciani/` (§6 della sessione 10/08)
- [ ] Aggiornare `GUIDA_SERVER.md` **dopo** i passi 0–4

### BARLERA — client: il socket, l'utente corrente, i tre requisiti fermi

**Subito**

- [ ] ⛔ **Chiamare `connect()`**: il client non ha mai aperto un socket (S22, decisione 22).
      Connessione automatica all'avvio in `TheKnifeClient.start()`, prima dello splash,
      nessun dialog. Host e porta da `getParameters().getRaw()` (decisione 27), default dalla
      costante di `theknife-common`. *Dipende da: la costante, che la porta Ciani*
- [ ] Errore leggibile se la connessione fallisce — è il primo messaggio che vedrà il
      docente se il server non è avviato. Deve dire **quale** host:porta ha tentato e la
      sintassi per riavviare. Non un dialog che chiede l'host: riaprirebbe la decisione 22
- [ ] `NumberFormatException` sull'argomento della porta: intercettata, messaggio leggibile,
      non stack trace
- [ ] Override di `Application.stop()` → `disconnect()`: oggi nessuno chiude il socket.
      Guardia sul socket `null`, altrimenti `ServerConnection:57` va in `NullPointerException`
      proprio quando `connect()` è fallito
- [ ] Campo `utenteCorrente` in `ServerConnection` accanto al `sessionToken`, valorizzato in
      `AuthService` da `loginResultDTO.getUtente()`, azzerato al logout (decisione 23)

**Poi**

- [ ] RF12/RF13: Modifica ed Elimina visibili solo sulle recensioni proprie del Cliente
- [ ] RF08/RF09: icona preferiti dinamica
- [ ] RF18: filtrare anche «Rispondi» per proprietà — oggi è visibile a chiunque
      (decisione 24). Resta cosmetico: il rifiuto vero lo fa il server
- [ ] Due righe di JavaDoc sull'`executor` di `TaskRunner` che spieghino perché è a thread
      singolo (decisione 25)

### SCOLARO — server: chiudere il layer DAO

- [x] ~~`PreferitoDAO` (3 metodi)~~ — **arrivato** (commit `b53ce04`). **S17 chiusa, il
      layer dati è completo.** Sblocca i 3 metodi sui preferiti di `RistoranteService`
- [ ] ⛔ **S29 — riallineare `Queries.sql` ai DAO**, tre punti verificati riga per riga:
      - righe 274 e 281: via `AND id_cliente = ?` (l'ownership non sta più nel DAO, sta nel
        service — vedi nota sotto la decisione 24)
      - riga 136: `LIMIT ?` → `WHERE distanza_km <= ?`
      - riga 242: `visualizzaPreferiti` a 8 colonne → le 18 di `trovaPerCliente`
      Il codice è già giusto in tutti e tre i punti: si cambia il file, non i DAO
- [ ] JavaDoc su ogni metodo pubblico dove manca (DIRETTIVE §3.2)
- [ ] Unificare la propria identità git: tre email/identità diverse in cronologia (S18)

Il resto del layer è consegnato e collaudato. Il riscontro del passo 0 di Ciani è stato
recepito e i difetti corretti. **Scolaro non blocca più nessuno.**

### GASPARINI — protocollo e documentazione

- [x] ~~S30 — decidere se `AggiungiRistoranteDTO` porta `provincia`~~ → **decisione 28**:
      sì, entra nel DTO insieme all'indirizzo obbligatorio
- [ ] ⛔ **Applicare la decisione 28**: aggiungere `provincia` a `AggiungiRistoranteDTO` e
      togliere lo `NULL` da `RistoranteDAO.inserisci()`. È un cambio di DTO serializzato:
      annunciare al gruppo al momento del push (stessa cautela della decisione 26)
- [x] ~~Portare in call la metà scoperta sulle coordinate~~ → **decisione 29**, chiusa in
      call il 12/08: geocoding lato server, nessun campo lat/lon nel DTO. Implementazione a
      Ciani (checklist sotto)
- [ ] S28: allineare lo snippet dell'enum in `ContrattoProtocolloCS.md:11` a
      `CommandType.java` — `ESCI` e `CERCA_VICINO` non stanno sotto `// CLIENTE`
- [ ] Proseguire il manuale tecnico
- [ ] Fornire a Ciani la tabella di autorizzazione in forma **definitiva**: è l'input
      diretto del gate della decisione 20, e va letta una volta sola

---

## 8. Dipendenze e ordine

```
Ciani costante rete ──►  Barlera connect()  ───┐
      (common)                                 ├──►  PRIMO END-TO-END
Ciani RistoranteService ──► cablaggio Server ──┘     (login dalla GUI)
        │                         │
        │                         └──►  Command di autenticazione
        │
        └──►  10 metodi, preferiti compresi (PreferitoDAO è arrivato, S17 chiusa)

Gasparini tabella      ──►  gate nel Dispatcher (S8)  ──►  ownership nei Command
                                                            (debito S29 da saldare qui)

Gasparini decisione 28 ──►  provincia nel DTO   ──┐
Ciani decisione 29      ──►  geocoding in aggiungi()  ├──►  AggiungiRistoranteCommand
```

Due catene indipendenti convergono sul primo end-to-end. Nessuna delle due è lunga; il
rischio non è la difficoltà, è che restino entrambe ferme in attesa dell'altra.

**La sola cosa che oggi blocca qualcun altro è la costante di rete in `theknife-common`**:
è mezz'ora di lavoro, non una decisione, e finché non esiste Barlera non ha da dove leggere
il default.

---

## 9. Nota di metodo

Il 10/08 avevamo imparato che *tradurre una specifica in una struttura dati la valida*. La
lezione di oggi è vicina ma distinta.

Sei giorni fa il difetto era il layer DAO mai eseguito. Oggi è il cablaggio mai scritto. In
entrambi i casi il codice compilava, il numero di classi cresceva, ogni membro poteva
mostrare lavoro fatto — e il sistema non aveva mai funzionato nemmeno una volta.

Il tratto comune è che **le parti si scrivono più in fretta delle connessioni fra le parti,
e solo le connessioni si accorgono degli errori.** Un DAO che nessuno chiama non ha bug
osservabili; uno stub che ritorna una lista vuota compila come uno che interroga il
database. Un progetto diviso in quattro persone tende naturalmente a produrre quattro
insiemi di parti e nessuna connessione, perché ogni connessione appartiene a due persone e
quindi a nessuna.

Il correttivo non è un processo, è una priorità: **il primo end-to-end vale più di qualunque
funzionalità aggiuntiva**, e va fatto appena è tecnicamente possibile, anche minimo, anche
brutto. Da quel momento ogni riga nuova viene provata; prima, nessuna.

# Sessione 04/08/2026 — Contratto DAO, apertura del layer dati, piano 3–4 giorni

## Perché questo documento

La sessione 03/08 aveva un obiettivo: riallineare il codice al contratto dati D1–D5
e far ripartire il parallelo. **Quell'obiettivo è raggiunto.** Common, SQL e client
hanno chiuso le rispettive checklist; il progetto compila per intero.

Ma il check di oggi fa emergere uno **sbilanciamento nuovo**: il client copre
**20 comandi su 20** del protocollo, il server ne implementa **1**. Il server non è
più "un modulo in parallelo agli altri": è il **collo di bottiglia unico** del
progetto.

Questo documento fa tre cose:

1. fotografa lo stato reale, file per file;
2. apre il **layer dati** definendo il contratto DAO, perché due persone possano
   lavorarci senza pestarsi;
3. dà a ciascuno una checklist da **3–4 giorni** che non dipende da nessun altro.

**Fonti che non si riaprono:** `DIRETTIVE_PROGETTO.md` (docente), contratto D1–D5
(`Sessione 30_07_2026.md` §2), decisioni 1–6 (`Sessione 03_08_2026.md` §1).

---

## 1. Stato verificato al 04/08

Verificato compilando ed esaminando i sorgenti, non per dichiarazione.

```
mvn -o clean compile  →  BUILD SUCCESS  (theknife-common, theknife-server, theknife-client)
```

| Modulo | Stato | Note |
|--------|-------|------|
| `theknife-common` | ✅ **Completo** | 19 DTO + 3 enum + Request/Response. D1–D5 applicati integralmente |
| SQL (`Scolaro/Database`) | ✅ **Completo** | `Schema.sql` 67 righe allineato al contratto; `Data.sql` 63.024 righe con `Servizio` + ponte popolati |
| `theknife-client` | ✅ **Avanti** | 12 schermate FXML + 12 controller + `ServerConnection` + 3 service. **20/20 comandi coperti** |
| `theknife-server` | ⚠️ **1/20** | Rete e dispatcher funzionanti, ma **1 solo comando** implementato, **0 DAO**, **0 connessioni al DB** |

### Checklist 03/08 — esito

| Owner | Esito |
|-------|-------|
| **Gasparini (common)** | ✅ **7/7 chiusi.** `idGestore: Long` ✓, `usernameAutore` rimosso ✓, `LocalDateTime` ✓, `OTTIENI_STATISTICHE_RISTORANTE` ✓, JavaDoc `ResponseStatus` ERRORE/ERRORE_SERVER ✓ |
| **Scolaro (SQL)** | ✅ **9/9 chiusi.** `provincia` ✓, `fascia_prezzo SMALLINT CHECK 1..4` ✓, colonna `servizi` rimossa ✓, `id_gestore` FK ✓, `Servizio`+`RistoranteServizio` ✓, `id_cliente` FK ✓, `UNIQUE(id_cliente,id_ristorante)` ✓, `Preferiti` PK composta ✓ |
| **Barlera (client)** | ✅ **Oltre il previsto.** 12/12 schermate + `client.network` e `client.service` completi, non solo scheletri |
| **Ciani (server)** | ⚠️ **Parziale.** Stub ✓, FASE 2 networking ✓. Manca il test end-to-end; FASE 5 (DAO) non iniziata |

### Cosa c'è davvero nel server

**Funzionante:**
- `TheKnifeServer.main` → `ConfigurazioneDB.leggiDaTerminale()` → `Server.avvia()`
- `Server.avvia()`: `ServerSocket(9999)`, `newFixedThreadPool(20)`, ciclo `accept()`
- `ClientHandler`: `ObjectOutputStream` prima di `ObjectInputStream` (ordine corretto,
  evita il deadlock degli header), ciclo `readObject`/`dispatch`/`writeObject`,
  `EOFException` gestita come chiusura normale
- `CommandDispatcher`: risolve il token via `SessionManager`, crea il comando, esegue
- `SessionManager`: `ConcurrentHashMap<String, UtenteDTO>` — thread-safe, corretto
- Eccezioni: `ApplicationException`, `ValidationException`, `UnauthorizedException`

**Mancante:**
- `CommandFactory` risolve **1 caso su 20** (`CERCA_RISTORANTI`); gli altri 19 cadono
  in `default:` → `IllegalArgumentException`
- `RistoranteService.cercaRistoranti()` ritorna `new ArrayList<>()` — stub
- Package `server.dao`: **non esiste**
- `Server.config` è **assegnato e mai letto**: il server non apre nessuna connessione
  al database, né verifica le credenziali che ha appena chiesto all'utente
- Non esistono `UtenteService`, `RecensioneService` lato server

---

## 2. Il quadro nuovo: dove passa la linea critica

```
CLIENT   ████████████████████  20/20 comandi
COMMON   ████████████████████  completo
SQL      ████████████████████  completo
SERVER   █                      1/20 comandi
```

Conseguenza operativa: **ogni schermata del client è pronta a parlare e non ha con
chi.** Barlera può agganciare i controller ai service, ma il primo test reale contro
il server fallirà su 19 comandi su 20.

Per questo la sessione di oggi sposta **Scolaro dentro il server**, sul layer DAO.
Il layer dati è il pezzo più voluminoso rimasto (5 DAO, ~25 metodi) ed è anche quello
che Scolaro può scrivere meglio di chiunque: conosce schema e query, le ha scritte lui
(`Queries.sql`, 260 righe già pronte).

---

## 3. Decisioni da prendere in questa call

Numerate per poterle citare. Per ognuna c'è una raccomandazione, ma **decide il
gruppo**.

| # | Tema | Opzioni | Raccomandato |
|---|------|---------|--------------|
| **7** | Proprietà del modulo server | (a) resta solo Ciani (b) **Ciani rete/service/command + Scolaro `server.dao`** | **(b)** — vedi §4 per il confine |
| **8** | Strategia di connessione | (a) **una connessione per operazione** (`try-with-resources`) (b) pool di connessioni | **(a)** — corretta sotto i 20 thread, zero dipendenze esterne, nessun bug di rilascio |
| **9** | Verifica credenziali DB | (a) lazy, al primo uso (b) **fail-fast all'avvio con retry (max 3)** | **(b)** — copre DIRETTIVE §1 e dà diagnostica su host/DB/utente sbagliati |
| **10** | Hash password utenti | (a) in chiaro (b) SHA-256 (c) BCrypt | **(b)** — è già scritto nel SequenceDiagram, va solo ratificato qui. (c) è più solido ma aggiunge una dipendenza in `lib/` da documentare in `README.txt` |
| **11** | Creazione ruolo Postgres | (a) **script `CREATE ROLE` con credenziali note** (b) superuser esistente del docente | **(a)** — deterministico, permessi limitati a `dbTK`, il `README.txt` può dire cosa digitare |
| **12** | Generazione del sessionToken | `UUID.randomUUID().toString()` dentro `AccediCommand` | conferma |

### Nota su #10

`UtenteDTO` **non ha un campo password** — è deliberato e va tenuto così: quel DTO
viaggia sul socket verso il client, e una password (anche hashata) non deve uscire dal
server. Conseguenza sul DAO: serve un metodo dedicato che restituisca **solo l'hash**,
separato da quello che restituisce il DTO. È il motivo della firma `trovaHashPassword`
in §4.

---

## 4. Contratto DAO — il pezzo che rende indipendenti Scolaro e Ciani

Questo è il cuore del documento. Se questo contratto è fisso, **Scolaro scrive i DAO e
Ciani scrive i Service senza mai vedersi**, poi le due metà si incastrano.

### Confine di proprietà (decisione 7)

| Chi | Cosa tocca | Cosa NON tocca |
|-----|-----------|----------------|
| **Ciani** | `server.network`, `server.handler`, `server.command`, `server.service`, `server.exception`, `ConfigurazioneDB`, `DatabaseManager` | ❌ nessun file dentro `server/dao/` |
| **Scolaro** | **solo** `server/dao/` (5 file nuovi) | ❌ nessun file fuori da lì |

Zero file in comune ⇒ **zero conflitti git**. Ognuno crea file nuovi nella propria
cartella.

### Il seam: `DatabaseManager`

`server.dao.DatabaseManager` — **lo scrive Ciani per primo** (è ~40 righe, mezz'ora),
lo pusha, e da quel momento Scolaro è libero per 3 giorni.

Responsabilità: costruire l'URL `jdbc:postgresql://<host>:5432/dbTK` da
`ConfigurazioneDB`, e fornire `Connection getConnection() throws SQLException`.
Nient'altro — nessun SQL, nessuna logica.

I DAO lo ricevono nel costruttore. Non lo istanziano, non lo cercano.

### Regole non negoziabili per chi scrive un DAO

1. **`PreparedStatement` sempre.** Mai concatenare stringhe in SQL: un client che manda
   `Roma'; DROP TABLE Recensioni; --` cancella il database. Ogni parametro è un `?`.
2. **`try-with-resources`** su `Connection`, `PreparedStatement`, `ResultSet`. Non li
   libera il garbage collector: connessioni non chiuse ⇒ Postgres satura
   `max_connections` e il server smette di rispondere senza errori evidenti.
3. **`SQLException` non esce dal DAO.** Si cattura e si riavvolge in
   `ApplicationException` (che i Command già gestiscono). Se `SQLException` arriva al
   Service, il layering è rotto.
4. **Niente logica di dominio.** Il DAO non valida, non autorizza, non decide. Riceve
   dati già validi e li scrive; legge righe e le impacchetta in DTO.
5. **Niente `theknife.common.protocol`.** Un DAO che importa `Request`/`Response` è un
   errore di design: il layer dati non sa che esiste una rete.

### Firme dei 5 DAO

Ratificare in call, poi **congelare**. Se una firma va cambiata dopo, si segnala qui,
non si cambia da soli.

```
UtenteDAO(DatabaseManager db)
    UtenteDTO  trovaPerUsername(String username)          // null se assente
    UtenteDTO  trovaPerId(long idUtente)
    String     trovaHashPassword(String username)          // solo hash, mai nel DTO (§3 #10)
    long       inserisci(RegistrazioneDTO dati, String hashPassword)   // ritorna id generato
    boolean    usernameEsiste(String username)

RistoranteDAO(DatabaseManager db)
    List<RistoranteDTO>       cerca(CercaRistorantiDTO filtri)
    List<RistoranteDTO>       cercaVicino(double lat, double lon, double raggioKm)
    RistoranteDTO             trovaPerId(long idRistorante)
    List<RistoranteDTO>       trovaPerGestore(long idGestore)
    long                      inserisci(AggiungiRistoranteDTO dati, long idGestore)
    void                      associaGestore(long idRistorante, long idGestore)
    StatisticheRistoranteDTO  statistiche(long idRistorante)

RecensioneDAO(DatabaseManager db)
    List<RecensioneDTO>  trovaPerRistorante(long idRistorante)
    List<RecensioneDTO>  trovaPerGestore(long idGestore)     // recensioni sui suoi ristoranti
    RecensioneDTO        trovaPerId(long idRecensione)
    long                 inserisci(AggiungiRecensioneDTO dati, long idUtente)
    void                 aggiorna(ModificaRecensioneDTO dati)
    void                 elimina(long idRecensione)
    void                 rispondi(RispondiRecensioneDTO dati)

PreferitoDAO(DatabaseManager db)
    void                 aggiungi(long idCliente, long idRistorante)
    void                 rimuovi(long idCliente, long idRistorante)
    List<RistoranteDTO>  trovaPerCliente(long idCliente)

ServizioDAO(DatabaseManager db)
    List<ServizioDTO>  tutti()
    List<ServizioDTO>  trovaPerRistorante(long idRistorante)
    void               associa(long idRistorante, List<ServizioDTO> servizi)
```

Tutti i metodi dichiarano `throws ApplicationException`.

### Tre trappole note nel mapping

Emerse confrontando `RistoranteDTO` con `Schema.sql`. Vanno risolte dentro il DAO, non
altrove.

**a) `mediaStelle` e `numeroRecensioni` non sono colonne.** Sono aggregati su
`Recensioni`. Servono `AVG(stelle)` e `COUNT(*)` con **`LEFT JOIN`** — con `INNER` i
ristoranti senza recensioni sparirebbero dai risultati di ricerca, che è la maggioranza
del dataset CSV.

**b) `servizi` è una `List` che viene da una tabella ponte.** `RistoranteServizio` è
N:N. Una query unica con join **duplica le righe del ristorante** (una per servizio):
va raggruppata in Java. L'alternativa — una query per ristorante — su una lista di
risultati diventa **N+1 query**. Scegliere consapevolmente, e scrivere quale nel
commento del metodo.

**c) `fascia_prezzo` è `SMALLINT`, il DTO ha `int`.** `getInt()` legge correttamente
uno `SMALLINT`, nessuna conversione necessaria. Segnalato solo per evitare dubbi.

---

## 5. Checklist per persona — 3/4 giorni

Ordinate per priorità. Nessuna dipende da lavoro altrui, salvo dove indicato.

### CIANI — server: connessione, comandi, service

**Giorno 1 — sblocca Scolaro entro le prime ore**

- [ ] `server.dao.DatabaseManager`: URL da `ConfigurazioneDB`, `getConnection()`
- [ ] **Push immediato** (Scolaro è bloccato finché non arriva)
- [ ] `Server.avvia()`: connessione di prova **prima** del `ServerSocket` (decisione 9)
- [ ] Retry credenziali, max 3 tentativi, messaggi distinti per SQLState:
      `28P01` password errata · `28000` utente inesistente · `3D000` `dbTK` assente ·
      `08001` host irraggiungibile
- [ ] ⚠️ `leggiDaTerminale()` fa `sc.close()` sullo `Scanner` di `System.in`: chiude lo
      stream sottostante, e al 2° tentativo **non si legge più nulla**. Va ripensato
      prima di implementare il retry — è un bug che si manifesta solo sbagliando la
      password, quindi mai in test e sempre in demo

**Giorno 2 — autenticazione end-to-end**

- [ ] `server.service.UtenteService`: `autentica()`, `registra()`; hash password
      (decisione 10); genera token `UUID.randomUUID()`
- [ ] `AccediCommand`, `RegistratiCommand`, `EsciCommand`
- [ ] Registrarli in `CommandFactory`
- [ ] **Test reale:** `serverTK` + `clientTK`, login da GUI, verificare che il token
      torni e che `SessionManager` lo registri

**Giorno 3–4 — comandi rimanenti**

- [ ] `RistoranteService` (server) sopra `RistoranteDAO`: ricerca, dettagli,
      statistiche, aggiunta, associazione, preferiti
- [ ] `RecensioneService` (server) sopra `RecensioneDAO`
- [ ] Restanti 16 comandi + registrazione in `CommandFactory`
- [ ] **Regola di autorizzazione:** `CommandDispatcher` passa `utente` che può essere
      `null` (guest). **Ogni Command che richiede autenticazione deve controllarlo** e
      lanciare `UnauthorizedException`. Non c'è un controllo centralizzato — se manca
      nel Command, il comando è aperto a chiunque
- [ ] Test concorrenza: 2+ client simultanei, verificare che le sessioni non si
      mescolino

### SCOLARO — server: layer DAO (nuovo incarico)

Attende solo il `DatabaseManager` di Ciani (giorno 1, mattina).

**Preparazione (subito, senza aspettare)**

- [ ] Rileggere §4: confine di proprietà, 5 regole, firme
- [ ] Verificare l'import reale su Postgres locale: `Schema.sql` poi `Data.sql`, senza
      errori — è anche il collaudo di ciò che eseguirà il docente
- [ ] Mappare le query di `Queries.sql` sui metodi di §4: molte sono già scritte

**Giorno 1–2**

- [ ] `UtenteDAO` (5 metodi) — sblocca il login di Ciani, **fallo per primo**
- [ ] `RistoranteDAO.cerca()` + `trovaPerId()` — attenzione alle trappole (a) e (b)
- [ ] `ServizioDAO` (3 metodi) — serve a `RistoranteDAO` per popolare `servizi`

**Giorno 3–4**

- [ ] `RistoranteDAO`: metodi rimanenti (gestore, inserimento, statistiche, `cercaVicino`)
- [ ] `RecensioneDAO` (7 metodi)
- [ ] `PreferitoDAO` (3 metodi)
- [ ] JavaDoc su ogni metodo pubblico (DIRETTIVE §3.2) + intestazione autore su ogni
      file (DIRETTIVE §3.5: nome, cognome, matricola, sede)

**Regola di consegna:** ogni DAO finito e compilato → commit + push singolo. Ciani ci
costruisce sopra man mano, non aspetta il blocco completo.

### GASPARINI — protocollo e i due buchi di contratto

Common è chiuso, ma il check ha trovato **due punti in cui il protocollo non copre un
requisito del docente**. Sono suoi perché toccano i DTO.

- [ ] **Buco 1 — `LoginResultDTO` è troppo povero.** Contiene solo `sessionToken` e
      `ruolo`. DIRETTIVE §1 chiede *"ristoranti vicini al domicilio in caso di utente
      loggato"*: il client non riceve mai `domicilio`, né il nome da mostrare in
      Home. Proposta: aggiungere `UtenteDTO utente`. **Decisione da prendere in call**
- [ ] **Buco 2 — `CercaVicinoDTO` contiene solo `raggioKm`.** Non porta nessuna
      posizione. Per un guest, DIRETTIVE §1 chiede *"il luogo indicato dall'utente
      guest"*: quel luogo non ha modo di arrivare al server. Inoltre
      `UtenteDTO.domicilio` è una `String`, non lat/long — per un utente loggato serve
      comunque una conversione. Opzioni: (a) aggiungere `PosizioneDTO` a
      `CercaVicinoDTO` e far geocodificare il client (b) aggiungere `String luogo` e
      geocodificare il server (`server.external.GeocodingClient`, previsto nel
      PackageDiagram, **non implementato e non assegnato a nessuno**).
      **Decisione da prendere in call** — impatta chi implementa, quindi va chiusa presto
- [ ] Dopo la decisione: applicare le modifiche ai DTO, `mvn -pl theknife-common install`,
      push, **avvisare tutti** (cambia la serializzazione: senza ripull si ottiene
      `InvalidClassException`)
- [ ] Aggiornare `ContrattoProtocolloCS.md` con la tabella completa dei 20 comandi:
      payload di richiesta → payload di risposta. È il riferimento che userà Ciani per
      i 16 comandi rimanenti
- [ ] Supporto documentale: iniziare il **Manuale Tecnico** (DIRETTIVE §5) — architettura
      a layer, protocollo, scelte di concorrenza

### BARLERA — client: aggancio GUI e primo end-to-end

Il layer di comunicazione è fatto; i controller sono ancora scollegati.

- [ ] Collegare `LoginController` e `RegistrazioneController` ad `AuthService` — è il
      **primo end-to-end reale** del progetto, da fare appena Ciani pusha
      `AccediCommand` (giorno 2)
- [ ] `HomeController` → `RistoranteService.cercaRistoranti()`; `RisultatiController` →
      `TableView`/lista popolata da `List<RistoranteDTO>`
- [ ] `DettaglioController` → `ottieniDettagli()` + `leggiRecensioni()`
- [ ] Gestione errori a video: `Response` con status ≠ `SUCCESSO` deve diventare un
      messaggio leggibile, **mai uno stack trace**
- [ ] ⚠️ **JavaFX si blocca:** `ServerConnection.inviaRichiesta()` è sincrona. Chiamata
      dal JavaFX Application Thread, **congela la GUI** finché il server non risponde.
      Serve `Task`/`Service` di JavaFX, con l'aggiornamento dei nodi in
      `Platform.runLater`. Da impostare **ora**, prima di scrivere 12 binding da rifare
- [ ] ⚠️ **`ServerConnection.getInstance()` non è thread-safe:** `if (instance == null)`
      senza sincronizzazione. Con più thread (vedi punto sopra) due thread possono
      creare due connessioni. Va reso sicuro — inizializzazione eager o `synchronized`
- [ ] Diagrammi: applicare gli esiti di §6

---

## 6. Diagrammi — verdetto sulla promozione

Verificati leggendo l'XML e confrontandolo con i sorgenti reali, campo per campo.

| Diagramma | Posizione attuale | Verdetto |
|-----------|-------------------|----------|
| **ClassDiagram** | `Barlera/Diagrammi/` | ✅ **PROMUOVI** |
| **StateDiagram** | già in `1_Doc. Conclusi/` | ✅ **CONFERMATO** — fix applicato |
| **SequenceDiagram** | `Barlera/Diagrammi/` | ⚠️ **PROMUOVI con nota** |
| **PackageDiagram** | già in `1_Doc. Conclusi/` | ⚠️ **RESTA, con nota** |
| **DiagrammaTK** (ER) | già in `1_Doc. Conclusi/` | ❌ **UNA CORREZIONE PRIMA** |

**ClassDiagram — promuovi.** Verificato campo per campo contro i DTO reali:
`UtenteDTO` (7 campi + `ruolo` come associazione), `RistoranteDTO` (18 campi, inclusi
`provincia` e `idGestore: Long`), `RecensioneDTO` (`titolo` presente,
`LocalDateTime` su entrambe le date, `usernameAutore` assente), `ServizioDTO`.
**Corrisponde al codice.** `servizi` e `ruolo` modellati come associazioni anziché
attributi: corretto in UML.

**StateDiagram — confermato.** Tre stati (`Non Autenticato`, `Cliente`, `Ristoratore`),
transizioni con guardia sul ruolo, self-transition guest sulla ricerca, `logout /
invalida token` coerente con `SessionManager.logout()`. Le celle flottanti segnalate il
03/08 non ci sono più. **Chiuso.**

**SequenceDiagram — promuovi, con una nota da verbalizzare.** 6 pagine, coerente con
l'architettura reale (`CommandDispatcher`, DTO italiani, `ResponseStatus` italiano).
Cita `AccediCommand`, `UtenteService`, `UtenteDAO` che **non esistono ancora**: va bene,
è un diagramma di progetto — ma quei nomi diventano **vincolanti** per chi implementa,
e sono già recepiti nelle checklist §5. Nota: la pagina Login contiene
*"calcola hash SHA-256(password ricevuta)"* — è la decisione 10, **presa in un diagramma
e mai verbalizzata**. Ratificarla in questa call.

**PackageDiagram — resta dov'è, con nota.** Mostra come architettura target
`server.dao` (5 DAO), `server.external` (`GeocodingClient`, `GeolocationClient`),
`UtenteService`, `RecensioneService`, `AccediCommand`: **nessuno implementato oggi**.
Legittimo per un diagramma di architettura, ma va scritto che è il *target*, non lo
stato. Due segnalazioni minori: (1) `server.external` non è assegnato a nessuno e non
compare in nessuna checklist — se le decisioni su §5/Gasparini/Buco 2 richiedono il
geocoding, va assegnato; (2) i package usano il prefisso `c.` (`c.server.dao`), mentre
CLAUDE.md prescrive la forma breve `server.dao`. Cosmetico, ma da uniformare.

**DiagrammaTK (ER) — una correzione prima di considerarlo chiuso.** L'entità
`Recensione` ha `Stelle, Data, Commento, Risposta, DataRisposta, ID` — **manca
`Titolo`**. Ma `titolo` è stato tenuto dalla decisione 5 del 03/08, esiste in
`Schema.sql`, in `RecensioneDTO` e nel ClassDiagram. L'ER è l'unico artefatto rimasto
indietro. Va aggiunto a mano in draw.io. *(Nota: `Location` come attributo composito
copre nazione/città/provincia — corretto in ER, non è un difetto.)*

### Azioni sui diagrammi

- [ ] **Barlera:** aggiungere `Titolo` a `Recensione` in `DiagrammaTK.drawio`
- [ ] **Barlera:** spostare `ClassDiagram.drawio` e `SequenceDiagram.drawio` in
      `Progettazione/1_Doc. Conclusi/Diagrammi/`
- [ ] **Ciani:** annotare nel Documento di Architettura che il PackageDiagram è il
      target, non lo stato al 04/08
- [ ] Ricordare: i `.drawio` si editano **solo a mano in draw.io** (regola CLAUDE.md)

---

## 7. Segnalazioni aperte

Emerse dal check, **non risolte di iniziativa**. Vanno chiuse dal gruppo.

| # | Segnalazione | Impatto | Owner proposto |
|---|--------------|---------|----------------|
| S1 | `LoginResultDTO` senza `UtenteDTO` → il client non riceve `domicilio` (DIRETTIVE §1) | **Alto** — requisito docente | Gasparini |
| S2 | `CercaVicinoDTO` senza posizione → il luogo del guest non arriva al server | **Alto** — requisito docente | Gasparini |
| S3 | `server.external` (geocoding) nel PackageDiagram, non implementato né assegnato | **Alto** se S2 → opzione (b) | da assegnare |
| S4 | Hash password deciso solo dentro un diagramma, mai verbalizzato | **Alto** — sicurezza | decisione 10 |
| S5 | `sc.close()` in `leggiDaTerminale()` chiude `System.in`: rompe il retry | Medio | Ciani |
| S6 | `ServerConnection.getInstance()` non thread-safe | Medio | Barlera |
| S7 | `inviaRichiesta()` sincrona sul thread JavaFX: congela la GUI | Medio | Barlera |
| S8 | Nessun controllo di autorizzazione centralizzato: ogni Command deve verificare `utente != null` | Medio | Ciani |
| S9 | Nessun comando di smoke test (il PING/PONG del 03/08 non è mai entrato in `CommandType`) | Basso | usare `CERCA_RISTORANTI` |
| S10 | Layout di consegna §4 (`doc/ bin/ src/ lib/`, `README.txt`) non ancora prodotto | Basso ora, **bloccante a fine progetto** | Ciani (PM) |

---

## 8. Dipendenze e ordine

```
Ciani: DatabaseManager (giorno 1, mattina)
   └──► sblocca Scolaro per 3 giorni interi

Scolaro: UtenteDAO (per primo)
   └──► sblocca AccediCommand di Ciani (giorno 2)
          └──► sblocca il primo end-to-end di Barlera

Gasparini: decisioni S1/S2 → fix DTO → push
   └──► ogni ripull obbligatorio (serializzazione)

Barlera: Task/Platform.runLater + fix Singleton
   └──► non aspetta nessuno, va fatto prima dei binding
```

**Un solo punto di sincronizzazione vero:** il `DatabaseManager` del giorno 1. Tutto il
resto scorre in parallelo.

**Attenzione alla serializzazione:** ogni modifica ai DTO va pushata e ripullata da
tutti prima di far dialogare client e server. Un DTO disallineato produce
`InvalidClassException` a runtime, non un errore di compilazione — quindi non lo vede
nessuno finché non si prova.

---

## 9. Nota di metodo

Il contratto D1–D5 e le decisioni 1–6 non si riaprono. Le decisioni 7–12 di oggi
seguono la stessa regola: chiuse in call, poi vincolanti.

**Nessuno tocca i package altrui.** Con Scolaro dentro `server.dao` il confine è più
stretto di prima ed è scritto in §4: si crea file nuovi nella propria cartella, mai si
modifica un file di un altro. Se serve una firma diversa da §4, si segnala qui e si
decide in gruppo.

Se emerge un'incoerenza non prevista: **si segnala, non si risolve da soli.**
